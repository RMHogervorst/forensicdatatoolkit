### ideas on how to extract the sprite thingies
### samples is matrix with every row a vector of 45
#select_row <- function(vec, x){vec[x,]}
#purrr::map(1:nrow(samples), ~select_row(samples, .x))
# no need, wrote helperfunction sprite_to_df()
